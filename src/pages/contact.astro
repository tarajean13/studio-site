---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Contact" description="Get in touch with Studio to discuss your next project.">
  <section class="px-6 lg:px-12 max-w-7xl mx-auto py-16 lg:py-24">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 lg:gap-24">
      <!-- Left Column -->
      <div>
        <h1 class="text-4xl md:text-5xl font-heading font-medium mb-8 leading-tight">
          Let's work together.
        </h1>
        <p class="text-lg text-secondary leading-relaxed mb-12">
          We're always interested in hearing about new projects. Whether you have a clear brief or just the beginning of an idea, we'd love to talk.
        </p>

        <div class="space-y-8">
          <div>
            <h3 class="text-sm uppercase tracking-wide text-muted mb-2">General Inquiries</h3>
            <a href="mailto:hello@studio.com" class="text-primary hover:text-secondary transition-colors">
              hello@studio.com
            </a>
          </div>
          <div>
            <h3 class="text-sm uppercase tracking-wide text-muted mb-2">New Business</h3>
            <a href="mailto:projects@studio.com" class="text-primary hover:text-secondary transition-colors">
              projects@studio.com
            </a>
          </div>
          <div>
            <h3 class="text-sm uppercase tracking-wide text-muted mb-2">Location</h3>
            <p class="text-secondary">
              Portland, Oregon<br />
              Available worldwide
            </p>
          </div>
        </div>
      </div>

      <!-- Right Column — Contact Form -->
      <div>
        <form
          name="contact"
          method="POST"
          data-netlify="true"
          class="space-y-8"
        >
          <input type="hidden" name="form-name" value="contact" />

          <div>
            <label for="name" class="block text-sm uppercase tracking-wide text-muted mb-2">
              Name
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full bg-transparent border-b border-border px-0 py-3 text-primary placeholder:text-muted focus:outline-none focus:border-primary transition-colors"
              placeholder="Your name"
            />
          </div>

          <div>
            <label for="email" class="block text-sm uppercase tracking-wide text-muted mb-2">
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full bg-transparent border-b border-border px-0 py-3 text-primary placeholder:text-muted focus:outline-none focus:border-primary transition-colors"
              placeholder="your@email.com"
            />
          </div>

          <div>
            <label for="project-type" class="block text-sm uppercase tracking-wide text-muted mb-2">
              Project Type
            </label>
            <select
              id="project-type"
              name="project-type"
              class="w-full bg-transparent border-b border-border px-0 py-3 text-primary focus:outline-none focus:border-primary transition-colors"
            >
              <option value="">Select a project type</option>
              <option value="branding">Brand Identity</option>
              <option value="web">Website Design</option>
              <option value="editorial">Editorial / Print</option>
              <option value="packaging">Packaging</option>
              <option value="motion">Motion</option>
              <option value="other">Other</option>
            </select>
          </div>

          <div>
            <label for="message" class="block text-sm uppercase tracking-wide text-muted mb-2">
              Message
            </label>
            <textarea
              id="message"
              name="message"
              rows="5"
              required
              class="w-full bg-transparent border-b border-border px-0 py-3 text-primary placeholder:text-muted focus:outline-none focus:border-primary transition-colors resize-none"
              placeholder="Tell us about your project"
            ></textarea>
          </div>

          <button
            type="submit"
            class="inline-block bg-primary text-background px-8 py-3 text-sm uppercase tracking-wide hover:bg-secondary transition-colors"
          >
            Send Message
          </button>
        </form>
      </div>
    </div>
  </section>
</BaseLayout>
